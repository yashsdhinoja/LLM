# For-Each Loop (Direct Iteration)
# This is the most common way to iterate over a list when you only care about the items themselves, not their index numbers.

numbers = [10, 20, 30, 40, 50, 60]

for num in numbers:
    print(f"Nums : {num}")