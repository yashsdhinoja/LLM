import numpy as np

array = np.array([5, 10, 15, 20])
print(array)

zeros = np.zeros([3,4])
print(zeros)

ones = np.ones(6)
print(ones)

full = np.full((2,2), 99)
print(full)

arange = np.arange(10, 20, 2)
print(arange)

identity_matrices = np.eye(5)
print(identity_matrices)